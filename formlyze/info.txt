database and user: neuralio_formlyze_db
database password: x9wtLK=2C}vz

email:formlyze@mrshakil.com
email password: 8b}0(@^v~Pmy






const downloadExcel = async (formId) => {
    const response = await fetch(`/api/export-responses/${formId}/`);
    const blob = await response.blob();
    const url = window.URL.createObjectURL(blob);
    
    const a = document.createElement("a");
    a.href = url;
    a.download = `form_${formId}_responses.xlsx`;
    a.click();
    window.URL.revokeObjectURL(url);
};
